---
trigger: always_on
glob:
description: HireWire workflow and rules
---

This workspace is HireWire. Follow `AGENTS.md` at the workspace root: it lists the stages and the rules. Before starting a stage, read its workflow file in `.claude/skills/<stage>/SKILL.md`.
